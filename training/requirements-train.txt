# Training stack for training/train_lora_v2.py.  Install on top of a CUDA torch
# build (RunPod PyTorch image).  Exact versions used for the paper's adapter are
# recorded in checkpoints/<adapter>/versions.json and in training/RUNPOD.md.
transformers>=4.51,<6
peft>=0.15
datasets>=3.0
accelerate>=1.0
huggingface_hub>=0.30
pytest>=8
